---
services:
  aura-api:
    container_name: aura-api
    image: ghcr.io/adm-metaex/aura-api/aura-api:v0.0.4-f6bf615
    restart: unless-stopped
    #    entrypoint: "sleep 600"
    tty: true
    ports:
      - "${API_PORT}:${API_PORT}"
      - "${API_SWAGGER_PORT}:${API_SWAGGER_PORT}"
    volumes:
      - ./creds/API_CERT_FILE:/creds/API_CERT_FILE:ro
      - ./ssl:/ssl
    env_file:
      - .env
    environment:
      HOST_HOSTNAME: "${HOST_HOSTNAME}"
    command:
      - -log=debug
    logging:
      options:
        max-size: "1g"
    networks:
      - aura-gw
    labels:
      - aura-gw=healthy
networks:
  aura-gw:
    driver: bridge
    name: aura-gw
